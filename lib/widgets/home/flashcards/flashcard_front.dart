import 'package:flutter/material.dart';

class FlashcardFront extends StatelessWidget {
  const FlashcardFront({super.key});

  @override
  Widget build(BuildContext context) {
    return  Text('front');
  }
}